{
  "name": "voyagermaps-www",
  "version": "1.0.0",
  "private": true,
  "description": "getvoyagermaps.com — static site generated by tools/build-pages.py and served from main by GitHub Pages",
  "scripts": {
    "build": "python3 tools/build-pages.py",
    "check": "npm run check:keys && npm run check:layout",
    "check:keys": "python3 tools/check-keys.py",
    "check:layout": "bash tools/check-layout.sh",
    "serve": "python3 -m http.server 8899 --bind 127.0.0.1"
  },
  "devDependencies": {
    "puppeteer": "^25.10.0"
  }
}
